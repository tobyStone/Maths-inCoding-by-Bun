<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Maths questions starter</title>
    <!-- Favicon -->
    <link rel="icon" type="image/png" href="/public/images/linux_site_logo.jpg" sizes="32x32">
</head>

<body>
    <!--questions for the Geometry in Scratch Physics simulator-->
    <div>
        <form id="question-form">
            <img id="question1_img" src="../images/question_images/<%= questionData.image1 %>" width="525" height="350" />
            <br>
            <span class="label">Choose one</span>
            <br>
            <input type="checkbox" name="answer" class="1" value="<%= questionData.answer1a %>">
            <label for="1a"><%= questionData.choice1a %></label>
            <input type="checkbox" name="answer" class="1" value="<%= questionData.answer1b %>">
            <label for="1b"><%= questionData.choice1b %></label>
            <input type="checkbox" name="answer" class="1" value="<%= questionData.answer1c %>">
            <label for="1c"><%= questionData.choice1c %></label>
            <input type="checkbox" name="answer" class="1" value="<%= questionData.answer1d %>">
            <label for="1d"><%= questionData.choice1d %></label>
            <br>
            <div>
                <img id="question2_img" src="../images/question_images/<%= questionData.image2 %>" width="525" height="350" />
                <br>
                <span class="label">Choose one</span>
                <br>
                <input type="checkbox" name="answer" class="2" value="<%= questionData.answer2a %>">
                <label for="2a"><%= questionData.choice2a %></label>
                <input type="checkbox" name="answer" class="2" value="<%= questionData.answer2b %>">
                <label for="2b"><%= questionData.choice2b %></label>
                <input type="checkbox" name="answer" class="2" value="<%= questionData.answer2c %>">
                <label for="2c"><%= questionData.choice2c %></label>
                <input type="checkbox" name="answer" class="2" value="<%= questionData.answer2d %>">
                <label for="2d"><%= questionData.choice2d %></label>
            </div>
            <br>
            <div>
                <img id="question3_img" src="../images/question_images/<%= questionData.image3 %>" width="525" height="350" />
                <br>
                <span class="label">Choose one</span>
                <br>
                <input type="checkbox" name="answer" class="3" value="<%= questionData.answer3a %>">
                <label for="3a"><%= questionData.choice3a %></label>
                <input type="checkbox" name="answer" class="3" value="<%= questionData.answer3b %>">
                <label for="3b"><%= questionData.choice3b %></label>
                <input type="checkbox" name="answer" class="3" value="<%= questionData.answer3c %>">
                <label for="3c"><%= questionData.choice3c %></label>
                <input type="checkbox" name="answer" class="3" value="<%= questionData.answer3d %>">
                <label for="3d"><%= questionData.choice3d %></label>
                <button type="submit">Send answer</button>
            </div>
        </form>
        <script src="anglesGrade.js"></script>
        <script>
            // JavaScript logic to keep track of the score and redirect based on it
            const questionForm = document.getElementById("question-form");
            questionForm.addEventListener("submit", function (event) {
                event.preventDefault(); // Prevent form submission
                const answers = questionForm.querySelectorAll("input[type=checkbox]:checked");
                const score = answers.length; // Calculate the score

                        <% if (score >= 2) { %>
                    // Redirect to a different video if the score is greater than or equal to 2
                    window.location.href = 'different_video.html';
                        <% } else { %>
                    // Redirect back to the original video player
                    window.location.href = 'original_video_player.html';
                        <% } %>
                    });
        </script>
    </div>
</body>
</html>
